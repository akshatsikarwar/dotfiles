#!/bin/bash

DB=$1
shift
if [[ $DB == "pmux" ]]; then
    #exec 3<>/dev/tcp/localhost/5105
    exec 3<>/dev/tcp/localhost/19900
    echo "$*" >&3
    cat <&3 &
    sleep 0.05
    kill %1
else
    cdb2sql -s $DB local "@send $*" | sed "s/^(out='//g;s/')$//g"
fi
