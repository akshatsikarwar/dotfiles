#!/usr/bin/env bash

#highlight a pane
if [[ ${1} = "on" ]]; then
  #tmux set-option window-status-style "bg=colour124"
  tmux set-option pane-active-border-style "fg=colour124"
else
  #tmux set-option window-status-style "bg=colour106"
  tmux set-option pane-active-border-style "fg=colour106"
fi
